/** @format */

import { StyleSheet, Text, View } from "react-native";
import React from "react";
import { ThemeView, ThemeText } from "../components/Themed";

const Articles = () => {
	return (
		<ThemeView>
			<ThemeText>Articles</ThemeText>
		</ThemeView>
	);
};

export default Articles;

const styles = StyleSheet.create({});
