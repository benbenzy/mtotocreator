/** @format */

import { StyleSheet, Text, View } from "react-native";
import React from "react";
import { ThemeText, ThemeView } from "../components/Themed";

const Messages = () => {
	return (
		<ThemeView>
			<ThemeText>Messages</ThemeText>
		</ThemeView>
	);
};

export default Messages;

const styles = StyleSheet.create({});
